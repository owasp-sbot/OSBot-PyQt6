name: CI Pipeline - DEV
on:
  workflow_dispatch:
  push:
    branches:
      - dev

env:
  GIT__BRANCH      : 'dev'
  RELEASE_TYPE     : 'minor'
  PACKAGE_NAME     : 'owasp_pyqt6'

jobs:

#  run-tests:
#    name: "Run tests"
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - name: "run-tests"
#        uses: owasp-sbot/OSBot-GitHub-Actions/.github/actions/pytest__run-tests@dev
#
#  increment-tag:
#    name: Increment Tag - DEV
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - name: Increment Tag
#        uses: owasp-sbot/OSBot-GitHub-Actions/.github/actions/git__increment-tag@dev
#        with:
#          release_type: ${{ env.RELEASE_TYPE }}
#    needs:
#      - run-tests
#
#  publish-to-pypi:
#    if: False
#    name: "Publish to: PYPI"
#    permissions:
#      id-token: write
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - name: Git Update Current Branch
#        uses: owasp-sbot/OSBot-GitHub-Actions/.github/actions/git__update_branch@dev
#      - name: publish-to-pypi
#        uses: owasp-sbot/OSBot-GitHub-Actions/.github/actions/pypi__twine__publish@dev
#    needs:
#      - increment-tag

 # Build macOS application and DMG
  pyqt6__build-dist__macos:
    name: "Build macOS Application"
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Build macOS application
        run: |
          cd owasp_pyqt6
          python build_macos.py

      - name: List build output
        run: |
          echo "Listing dist directory contents:"
          find owasp_pyqt6/dist -type f | sort

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-app-dmg
          path: |
            owasp_pyqt6/dist/*.dmg
            owasp_pyqt6/dist/*.app
          if-no-files-found: error
#    needs:
#      - increment-tag
